extends layout

block content
  .users-container
    //- Header
    .users-header
      h1.users-title Gestión de Usuarios
      p.users-subtitle Administra todos los usuarios del sistema

    //- Filtros
    .users-filters
      .filter-group
        label(for='roleFilter') Filtrar por rol:
        select#roleFilter.filter-select
          option(value='all') Todos
          option(value='user') Usuario
          option(value='admin') Admin

    //- Tabla de usuarios
    .table-wrapper
      table.users-table
        thead
          tr
            th
              span(data-sort-state='none') ID
              .sort-icon ↑
              .sort-icon ↓
              .sort-icon ↕
            th
              span(data-sort-state='none') Usuario
              .sort-icon ↑
              .sort-icon ↓
              .sort-icon ↕
            th
              span(data-sort-state='none') Rol
              .sort-icon ↑
              .sort-icon ↓
              .sort-icon ↕
            th
              span(data-sort-state='none') Creado
              .sort-icon ↑
              .sort-icon ↓
              .sort-icon ↕
            th Acciones

        tbody#usersTableBody
          tr
            td(colspan='5').loading Cargando usuarios...

    //- Paginación
    .users-pagination
      #pageInfo Página 1 de 1
      .pagination-controls
        button#prevBtn.pagination-btn(onclick='previousPage()' disabled) ‹ Anterior
        button#nextBtn.pagination-btn(onclick='nextPage()') Siguiente ›

block scripts
  script(src='/js/multiColumnSort.js')
  script(src='/js/users.js')
